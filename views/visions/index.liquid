{% layout "layout.liquid" %} {% block content %}

<div>
  <a href="/visions/new" class="newBtn"><button>Create a New Vision</button></a>
  
  {% for vision in visions %}
  <div class="container">
    <div class="row">
        <div class="column">
     <a href="/visions/{{vision._id}}"> <img src="{{ vision.image }}" /></a>
      {% if vision.complete == true %} {% else %} {% endif %}
    </div>
    </div>
  </div>
  {% endfor %}
</div>

{% endblock %}
